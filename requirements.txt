# CameraCopy runtime dependencies
# Target Python: 3.11+

PySide6>=6.7
psutil>=5.9
platformdirs>=4.2
python-dateutil>=2.9.0.post0

# Linux-only volume/device metadata
pyudev>=0.24.4; sys_platform == "linux"

# Windows-only richer disk/volume metadata
pywin32>=306; sys_platform == "win32"
WMI>=1.5.1; sys_platform == "win32"
